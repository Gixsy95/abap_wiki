# DEVELOPMENT dependencies (not runtime). They do NOT belong in requirements.lock.txt:
# the lockfile is frozen from a runtime-only environment (see freeze_lock.py) to
# stay minimal. Install with: pip install -r core/src/requirements-dev.txt
-r requirements.txt
pytest-cov>=5
ruff==0.15.20
